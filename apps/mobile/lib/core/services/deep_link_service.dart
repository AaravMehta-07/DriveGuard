class DeepLinkService {
  // - Parse incoming links (/destination/:id, /route/:hash, /enforcement/:id)
  // - Validate parameters (never trust arbitrary coordinates)
  // - Route to appropriate screen
}
